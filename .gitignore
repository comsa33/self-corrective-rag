# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
wheels/
*.egg

# Virtual environments
.venv/
venv/

# Environment
.env
.env.local

# Data (large files — tracked via DVC or manual download)
data/raw/
data/processed/
data/indices/
data/optimization/
# One results tree per model so runs never overwrite each other:
# results/=Gemini, results-openweight/=gpt-oss, results-glm/=glm, results-latency/=timings
data/results/
data/results-*/
# Human annotation sheets — the blank sheet and the filled labels behind the
# judge/human kappa. Release as supplementary material, not repo history.
data/human_eval/
human_eval.csv

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# Pytest
.pytest_cache/
htmlcov/
.coverage

# IDE
.idea/
.vscode/
*.swp

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.spl
*.synctex.gz
*.pdf
!paper/main.pdf
paper.zip

# Agent tooling state (project-specific config, not source)
.claude/
.omc/

# Paper source (private repo: comsa33/self-corrective-rag-paper)
paper/
paper-review/
overleaf-final.zip

# Internal docs (not for public)
REFACTORING.md
docs/KBS_GUIDELINES.md
docs/PAPER_PLAN.md
docs/Q1_READINESS_ASSESSMENT.md

# Presentation exports (not part of the public code release)
*.pptx

# Code review graph (auto-generated)
.code-review-graph/
