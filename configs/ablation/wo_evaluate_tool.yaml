# w/o Evaluate Tool — agent cannot assess passage quality (C2 ablation).
name: w/o Evaluate Tool
contribution: C2
pipeline: agentic

experiment:
  enable_agentic_refinement: true

agent:
  enabled_tools:
    - search
    - decompose
    - structure
    - terminology
    - inspect
