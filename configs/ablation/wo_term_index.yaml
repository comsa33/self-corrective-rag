# w/o Term Index — agent cannot map user terms to doc vocabulary (C3 ablation).
name: w/o Term Index
contribution: C3
pipeline: agentic

experiment:
  enable_agentic_refinement: true

agent:
  enabled_tools:
    - search
    - decompose
    - structure
    - evaluate
    - inspect
