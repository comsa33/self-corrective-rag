# w/o Section Index — agent cannot browse document structure (C3 ablation).
name: w/o Section Index
contribution: C3
pipeline: agentic

experiment:
  enable_agentic_refinement: true

agent:
  enabled_tools:
    - search
    - decompose
    - terminology
    - evaluate
    - inspect
