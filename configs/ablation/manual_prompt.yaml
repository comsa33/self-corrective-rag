# Manual Prompt — disable DSPy optimization, use manual prompts (C4 ablation).
name: Manual Prompt
contribution: C4
pipeline: agentic

experiment:
  enable_agentic_refinement: true
  enable_dspy: false
