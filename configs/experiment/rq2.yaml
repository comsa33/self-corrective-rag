# RQ2: How does the ReAct agent use its tools, and what tool-call
# patterns emerge during autonomous retrieval refinement?
#
# Analysis-focused RQ: trajectory analysis, tool call frequency,
# tool sequence patterns, search strategy evolution.
# Uses same Agentic pipeline but captures detailed trajectory data.

name: "RQ2: Tool Usage Analysis"
description: >
  Analyze ReAct agent tool-call trajectories including call frequency,
  sequence patterns, and strategic behavior across question types.

# Only one variant — the full agentic pipeline with trajectory capture
variants:
  - name: Agentic (Full Tools)
    pipeline: agentic
    experiment:
      enable_agentic_refinement: true
